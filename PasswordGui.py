import tkinter as tk

root = tk.Tk()
root.title('PassWord Strength Checker') # Changes the title of the window
root.geometry('600x600+50+50') # Changes the sze of the window
root.resizable(True) # Allows 

root.mainloop()
